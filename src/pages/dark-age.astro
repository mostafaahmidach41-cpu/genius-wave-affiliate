<style>
  .main-action-container {
    text-align: center;
    margin: 45px 0;
    padding: 10px;
  }

  .secure-checkout-btn {
    display: inline-block;
    padding: 22px 50px;
    font-size: 24px;
    font-weight: 900;
    text-decoration: none;
    color: #ffffff !important;
    background: linear-gradient(135deg, #d4145a 0%, #fbb03b 100%);
    border-radius: 12px;
    box-shadow: 0 10px 20px rgba(212, 20, 90, 0.3);
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    cursor: pointer;
    border: 2px solid rgba(255, 255, 255, 0.2);
    font-family: 'Arial Black', sans-serif;
    text-transform: uppercase;
    animation: attention-seeker 2.5s infinite;
  }

  .secure-checkout-btn:hover {
    transform: scale(1.1) rotate(-1deg);
    box-shadow: 0 15px 30px rgba(212, 20, 90, 0.5);
    background: linear-gradient(135deg, #fbb03b 0%, #d4145a 100%);
  }

  @keyframes attention-seeker {
    0% { transform: scale(1); }
    10% { transform: scale(1.05); }
    20% { transform: scale(1); }
    30% { transform: scale(1.05); }
    40% { transform: scale(1); }
    100% { transform: scale(1); }
  }
</style>

<div class="main-action-container">
  <a href="https://6b6803xomz0mrq81vg145pakft.hop.clickbank.net" class="secure-checkout-btn">
    Claim Your Discount - Watch Video Now!
  </a>
</div>
