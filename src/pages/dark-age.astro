<style>
  .cta-wrapper {
    text-align: center;
    margin: 50px 0;
  }

  .order-button-premium {
    display: inline-block;
    padding: 24px 60px;
    font-size: 26px;
    font-weight: 800;
    color: #ffffff !important;
    text-decoration: none;
    background: linear-gradient(135deg, #e63946 0%, #f1faee 100%);
    background: linear-gradient(45deg, #ff0000, #ffcc00);
    border-radius: 15px;
    box-shadow: 0 12px 24px rgba(255, 0, 0, 0.3);
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    border: 3px solid #ffffff;
    text-transform: uppercase;
    font-family: 'Arial Black', Gadget, sans-serif;
    animation: pulse-button 2s infinite;
  }

  .order-button-premium:hover {
    transform: scale(1.1);
    box-shadow: 0 18px 35px rgba(255, 0, 0, 0.5);
    filter: brightness(1.1);
  }

  @keyframes pulse-button {
    0% { transform: scale(1); }
    50% { transform: scale(1.03); }
    100% { transform: scale(1); }
  }
</style>

<div class="cta-wrapper">
  <a href="https://6b6803xomz0mrq81vg145pakft.hop.clickbank.net" class="order-button-premium">
    GET DARK AGE DEFENSE - 70% OFF NOW
  </a>
</div>
